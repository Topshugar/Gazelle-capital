GAZELLE PRO MT5 - Setup
1. pip install -r requirements.txt
2. python app.py -> http://localhost:5000
3. Admin user: register as 'admin' first account
Render: Connect repo, Build: pip install -r requirements.txt, Start: gunicorn app:app 
